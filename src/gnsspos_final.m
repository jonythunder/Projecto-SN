% Função que calcula a posição actual utilizando DGPS, recebendo os
% pseudo-ranges obtidos pelo recetor GPS e os erros calculados para cada
% satélite pela estação terrestre.
function xyz = gnsspos_final(last_pos,sat_pos,pr_current, satellites_prerrors)
	
end